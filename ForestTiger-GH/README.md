# Generated repository observations

This directory is managed by the Observatory workflows.

`repositories.csv`, `_collection/`, and one directory per automatically discovered `ForestTiger-GH` repository are created or updated by the collector. Do not manually maintain a repository list here; new repositories are discovered from GitHub on every run.
